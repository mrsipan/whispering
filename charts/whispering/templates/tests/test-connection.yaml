apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "whispering.fullname" . }}-test-connection"
  labels:
    {{- include "whispering.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "whispering.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
