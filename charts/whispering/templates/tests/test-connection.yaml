apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "docinsights.fullname" . }}-test-connection"
  labels:
    {{- include "docinsights.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "docinsights.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
