apiVersion: v1
kind: Service
metadata:
  name: {{ include "docinsights.fullname" . }}
  labels:
    {{- include "docinsights.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "docinsights.selectorLabels" . | nindent 4 }}
