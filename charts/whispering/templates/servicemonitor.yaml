apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: whispering-service-monitor-{{ .Release.Name }}
  labels:
    app: whispering
spec:
  selector:
    matchLabels:
      app.kubernetes.io/instance: {{ .Release.Name }}
      app.kubernetes.io/name: whispering
  endpoints:
  - port: http
    interval: 15s
